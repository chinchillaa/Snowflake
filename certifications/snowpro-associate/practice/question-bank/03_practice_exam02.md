# SnowPro Associate 確認問題 - Practice Exam 02

> 作成日: 2026-04-02
> 対象試験: SnowPro Associate: Platform Certification (SOL-C01)
> 問題数: 65問
>
> **試験概要:**
> - 問題数: 65問 / 時間: 85分 / 合格: 750点以上
> - 費用: $100 USD
> - 推奨経験: 3ヶ月以上

---

## 使い方

1. 各問題の「あなたの回答」欄に答えを記入
2. 「選んだ理由・メモ」欄に根拠を記述
3. 学習後、「正解・解説」を開いて自己採点
4. 理解度を振り返り、弱点を把握

---

## 問題1: アーキテクチャを構成する3つの主要なレイヤー（層）

Snowflakeのアーキテクチャを構成する3つの主要なレイヤー（層）は何ですか？

- A) データリポジトリ、コンピュートクラスター、メタデータ管理
- B) 認証、アクセス制御、クラウドサービス
- C) データベースストレージ、ODBCドライバー、仮想ウェアハウス
- D) データストレージ、クエリ処理、クラウドサービス

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: D) データストレージ、クエリ処理、クラウドサービス**

Snowflakeのアーキテクチャは、データの保存、計算処理、そして管理サービスを分離した3つの独立したレイヤーで構成されています。

- **データストレージ（Data Storage）**：すべての構造化データおよび半構造化データを、圧縮された列指向（カラムナ）フォーマットで保存します（列指向＝列ごとにデータをまとめて保存する形式で、特定の列だけを読み込めるため分析クエリに最適）。ファイルの構成やメタデータ、最適化はSnowflakeが自動で管理します。
    
- **クエリ処理（Query Processing）**：「仮想ウェアハウス」が担当する層であり、クエリの実行やデータの変換など、すべての計算処理を行います。
    
- **クラウドサービス（Cloud Services）**：メタデータの管理、ユーザー認証、アクセス制御、クエリの最適化、インフラストラクチャの調整など、システム全体の管理を行います。
    

このように各層が分離されているため、計算リソースとストレージをそれぞれ独立して拡張（スケール）させることができ、パフォーマンスの最適化と管理の簡素化が実現されています。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション1（アーキテクチャ概要）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題2: どのようにクエリを処理しますか？

Snowflakeはどのようにクエリを処理しますか？

- A) 共有ディスク型アーキテクチャを使用する
- B) MPP（超並列処理）コンピュートクラスターを使用する
- C) クラウドストレージ内のデータを最適化することで処理する
- D) サードパーティ製のコネクタを介して処理する

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) MPP（超並列処理）コンピュートクラスターを使用する**

Snowflakeは、MPP（Massively Parallel Processing：超並列処理）コンピュートクラスターを使用してクエリを処理します。これは「仮想ウェアハウス」として実装されています。各ウェアハウスは独立した計算ノードの集まりであり、手分けして並列に処理を行うことでクエリを高速に実行します。この仕組みにより、計算リソースを柔軟に拡張し、大規模なデータ処理にも高いパフォーマンスで対応できます。

❌ **共有ディスク型アーキテクチャ**：Snowflakeは共有ディスクとシェアードナッシング（非共有）の要素を組み合わせていますが、実際のクエリ実行はMPPクラスターが行います。

❌ **クラウドストレージ内のデータを最適化する**：ストレージの最適化も行われますが、クエリの「実行」自体はコンピュート（計算）レイヤーで行われます。

❌ **サードパーティ製のコネクタ**：コネクタはデータにアクセスするためのものであり、クエリを処理する仕組みではありません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション1（アーキテクチャ概要）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題3: Snowflakeがサポートしているクラウドプラットフォームはどれですか？

Snowflakeがサポートしているクラウドプラットフォームはどれですか？

- A) Amazon Web Services (AWS)
- B) Google Cloud Platform (GCP)
- C) Microsoft Azure
- D) すべての選択肢

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: D) すべての選択肢**

Snowflakeはクラウドネイティブなプラットフォームであり、AWS、GCP、Microsoft Azureのいずれの環境でも展開可能です。組織のインフラストラクチャやデータの保管場所（データレジデンシー）の要件に合わせて、最適なクラウドプロバイダーとリージョンを選択できます。

❌ **A) Amazon Web Services のみ**：AWSはSnowflakeが最初に対応したクラウドですが、現在はGCPとAzureも同様にサポートしています。単一のクラウドに限定する選択肢は、Snowflakeのマルチクラウド対応という特性を無視しているため不適切です。

❌ **B) Google Cloud Platform のみ** / **C) Microsoft Azure のみ**：同様に、どれか1つに限定した選択肢ではSnowflakeが稼働できる全環境を表せていません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション2（クラウド/リージョン）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題4: データを保存するためにどのようなストレージ形式を使用していますか？

Snowflakeはデータを保存するためにどのようなストレージ形式を使用していますか？

- A) 行指向（行ベース）フォーマット
- B) 圧縮された列指向（カラムナ）フォーマット
- C) 圧縮された行指向フォーマット
- D) 圧縮されていない行指向フォーマット

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) 圧縮された列指向（カラムナ）フォーマット**

Snowflakeは、データ分析処理に最適な「圧縮された列指向（カラムナ）フォーマット」でデータを保存します。この形式により、クエリの実行時に必要な列（カラム）だけを読み込むことができるため、パフォーマンスが大幅に向上し、ストレージコストも削減されます。さらに、強力な圧縮技術によってデータの物理的なサイズを最小限に抑えています。

❌ **行指向フォーマット / 圧縮された行指向フォーマット**：これらは通常の業務システム（OLTP）でよく使われる形式であり、大量のデータを集計する分析処理には非効率です。Snowflakeでは使用されません。

❌ **圧縮されていない行指向フォーマット**：非効率的であり、採用されていません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション1（アーキテクチャ概要）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題5: ユーザーインターフェース（UI）は何と呼ばれていますか？

Snowflakeのユーザーインターフェース（UI）は何と呼ばれていますか？

- A) Snowsite
- B) SnowGUI
- C) SnowView
- D) Snowsight

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: D) Snowsight**

Snowflakeの公式なWebインターフェースの名前は「Snowsight」です。SQLやPythonでの開発、データの視覚化、システムの監視、共同作業などをサポートするモダンなブラウザベースの画面です。以前の「クラシックコンソール」に代わり、現在ではデフォルトのインターフェースとなっています。

❌ **Snowsite, SnowGUI, SnowView**：これらは公式の製品名ではありません。

📖 **参照先:** まとめ/02_UI_Notebooks.md > セクション1（Snowsight）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. Snowflakeアカウントにログイン（https://app.snowflake.com）
2. ブラウザのタブタイトルや画面左上に「Snowsight」の名称が表示されている
3. 左メニューの各項目（Worksheets / Data / Monitoring / Admin など）を確認する

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題6: ノートブックでデータへのクエリや文書化に使用されるオプションはどれですか？（3つ選択）

Snowflakeノートブックでデータへのクエリや文書化に使用されるオプションはどれですか？（3つ選択）

- A) Python
- B) SQL
- C) Markup
- D) Markdown

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) Python、B) SQL、D) Markdown**

Snowflakeノートブックでは、以下の種類のセルを組み合わせて作業を行います。

- **Python**：Snowparkや標準ライブラリを使用して、データの操作や分析を行うコードを記述できます。
    
- **SQL**：Snowflakeのテーブルに対して直接クエリを実行するためのセルです。
    
- **Markdown**：コードの説明や分析結果のメモなど、書式設定されたテキストを追加するために使用します。
    

❌ **Markup**：ノートブックでサポートされているセルの種類ではありません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Projects」→「Notebooks」を開く
2. 既存のノートブックを開くか、「+ Notebook」で新規作成する
3. 「+ Add Cell」ボタンをクリックし、Python / SQL / Markdown の3種類が選べることを確認する
4. それぞれのセルを追加して動作を試す

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題7: Snowflakeノートブックの主な特徴は何ですか？

Snowflakeノートブックの主な特徴は何ですか？

- A) コマンドラインインターフェース（CLI）のみで提供される。
- B) データをテキストでのみ表示する。
- C) クエリの作成と結果の視覚化を統合した環境である。
- D) ハードウェアを手動で構成する必要がある。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: C) クエリの作成と結果の視覚化を統合した環境である。**

Snowflakeノートブックは、SQLとPythonのコード記述、データの視覚化（グラフ化）、およびMarkdownを用いたドキュメント作成をすべて1つの場所で行える対話型の開発環境です。ツールを切り替えたりインフラを手動で設定したりすることなく、データの探索や機械学習のワークフローをスムーズに進めることができます。

❌ **コマンドラインインターフェースのみ**：Webブラウザベースのインターフェースです。

❌ **データをテキストでのみ表示する**：グラフなどのリッチな視覚化をサポートしています。

❌ **ハードウェアの手動構成**：計算リソースはシステムが管理するため、手動の設定は不要です。

📖 **参照先:** まとめ/02_UI_Notebooks.md > セクション3（Notebooks）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 「Projects」→「Notebooks」でノートブックを開く
2. SQL セル・Python セル・Markdown セルをそれぞれ追加して実行する
3. Python セルで `import pandas as pd` や Streamlit の `import streamlit as st` を試す
4. Markdown セルで `# 見出し` や `**太字**` のような書式を入力してレンダリングを確認する

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題8: Pythonワークシートの設定項目で利用可能なオプション（2つ選択）

Pythonワークシートの設定項目で利用可能なオプションはどれですか？（2つ選択）

- A) Handler（ハンドラー）
- B) Return Value（戻り値）
- C) Return Type（戻り値の型）
- D) Output（出力）

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) Handler（ハンドラー）、C) Return Type（戻り値の型）**

SnowflakeのPythonワークシートの設定において、以下の2つを指定します。

- **Handler（ハンドラー）**：コードを実行する際の「開始地点（エントリーポイント）」となる関数名です。特にコードをストアドプロシージャとして保存・実行する場合に重要です。
    
- **Return Type（戻り値の型）**：関数が最終的に返すデータの形式（例：`STRING`、`TABLE`、`VARIANT`など）を定義します。これにより、Snowflakeが結果を正しく解釈して表示できます。
    

❌ **Return Value（戻り値）**：これは関数が実行された結果そのものであり、設定項目ではありません。

❌ **Output（出力）**：結果の表示部分を指す言葉ですが、設定する項目ではありません。

📖 **参照先:** まとめ/02_UI_Notebooks.md > セクション1（Snowsight）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 「Projects」→「Worksheets」で新しいワークシートを作成する
2. 右上の言語セレクターで「Python」を選択する
3. 右側の設定パネルに「Handler」と「Return Type」の入力欄が表示されていることを確認する
4. Handler に `main`、Return Type に `TABLE(col1 VARCHAR)` などを入力して動作を確認する

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題9: Snowflakeノートブックにおいて、セルの横にある赤色のチェックマークは何を意味しますか？

Snowflakeノートブックにおいて、セルの横にある赤色のチェックマークは何を意味しますか？

- A) セルにエラーがある
- B) セルが現在実行中である
- C) セルの実行が正常に完了した
- D) セルが一時停止中である

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) セルにエラーがある**

Snowflakeノートブックでは、セルの横に表示される「赤色のチェックマーク（または×印）」は、そのセルの実行中にエラーが発生したことを示します。これにより、どのコードが失敗したかをすばやく特定し、エラーメッセージを確認して修正を行うことができます。

❌ **セルが現在実行中である**：通常は回転するインジケーター（スピナー）が表示されます。

❌ **セルの実行が正常に完了した**：緑色のチェックマークが表示されます。

❌ **セルが一時停止中である**：ノートブックには標準機能として「一時停止」状態はありません。

📖 **参照先:** まとめ/02_UI_Notebooks.md > セクション3（Notebooks）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 「Projects」→「Notebooks」でノートブックを開く
2. Python セルに意図的にエラーのあるコードを入力する（例：`print(undefined_variable)`）
3. セルを実行して、セル左横に表示される赤い×印（エラーマーク）を確認する
4. 正常に実行が完了したセルには緑のチェックマークが表示されることも確認する

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題10: ユーザーはSnowflakeノートブックにStreamlitをどのように統合できますか？

ユーザーはSnowflakeノートブックに Streamlit をどのように統合できますか？

- A) HTMLコードを書くことによって。
- B) 手動でスクリプトを作成することによって。
- C) ネットワーク設定を構成することによって。
- D) ノートブック内のPythonコードでStreamlit関数を使用することによって。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: D) ノートブック内のPythonコードでStreamlit関数を使用することによって。**

SnowflakeノートブックはStreamlitとネイティブに統合されています。ノートブック環境を離れることなく、Pythonセルの中で `st.write()`、`st.dataframe()`、`st.slider()` といったStreamlitの関数を呼び出すだけで、対話型のダッシュボードや動的なデータ視覚化アプリを直接作成できます。

❌ **HTMLコードを書く**：UIの構築はPythonコードを通じてStreamlitが行うため不要です。

❌ **手動でスクリプトを作成する**：ノートブック内でシームレスに機能するため、外部のスクリプトは不要です。

❌ **ネットワーク設定を構成する**：接続はSnowflake内部で自動管理されます。

📖 **参照先:** まとめ/02_UI_Notebooks.md > セクション1（Snowsight）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. ノートブックを開き、Python セルで Streamlit をインポートする
2. 以下のコードを入力して実行し、インタラクティブなUIがセル出力として表示されることを確認する

**SQLでの確認:**
```python
import streamlit as st

# スライダーの表示
value = st.slider("値を選んでください", 0, 100, 50)
st.write(f"選択した値: {value}")

# データフレームの表示
import pandas as pd
df = pd.DataFrame({"列A": [1, 2, 3], "列B": ["a", "b", "c"]})
st.dataframe(df)
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題11: ノートブックにおいて、Pythonの変数をSQLに埋め込む方法は？

Snowflakeノートブックにおいて、Pythonの変数をSQLに埋め込む（変数置換を行う）にはどうすればよいですか？

- A) SQLクエリ内でプレースホルダーを使用する。
- B) HTMLコードを書く。
- C) データファイルを手動で編集する。
- D) ネットワーク設定を構成する。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) SQLクエリ内でプレースホルダーを使用する。**

Snowflakeノートブックでは、Pythonで定義した値（変数）を、プレースホルダーを用いてSQL文の中に直接埋め込むことができます。プレースホルダーとは `?` のような記号を使って「ここに値を後から代入する」という目印を置く方式で、例えば `SELECT * FROM orders WHERE status = ?` のように記述し、実行時にPython側の変数値を渡します。これにより、Python側の処理結果やユーザー入力を条件にして、SQLクエリを動的に組み立てることが可能になります。コードが整理され、柔軟な処理が実現できます。

❌ HTMLの記述、データファイルの編集、ネットワーク設定は、変数置換とは無関係の操作です。

📖 **参照先:** まとめ/02_UI_Notebooks.md > セクション3（Notebooks）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. ノートブックを開き、Python セルで変数を定義する
2. SQL セルでプレースホルダー `?` を使ってその変数を参照する

**SQLでの確認:**
```python
# Python セルで変数を定義
target_status = "active"
```
```sql
-- SQL セルでプレースホルダーを使用して Python 変数を参照
SELECT * FROM my_table WHERE status = ? LIMIT 10;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題12: Snowsightを使用して、ローカルのファイルからデータを読み込む際の機能は？

Snowsightを使用して、ローカル（自分のパソコン）のファイルからデータを読み込む際に利用できる機能はどれですか？

- A) ドラッグ＆ドロップのインターフェース
- B) コマンドラインからの入力
- C) 手動でのデータ入力
- D) クラウドストレージからの読み込みのみサポートしている

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) ドラッグ＆ドロップのインターフェース**

Snowsightの画面上には、ローカルのファイルをSnowflakeの内部ステージに直接アップロードできるドラッグ＆ドロップ機能が用意されています。「Data」メニューから「Add Data」→「Load files into a Stage」を選択すると、複数のファイルを一括で簡単にアップロードできます。

❌ **コマンドラインからの入力**：これは「SnowSQL」などの別ツールの機能です。

❌ **手動でのデータ入力**：大量のファイル読み込みには対応していません。

❌ **クラウドストレージのみサポート**：ローカルファイルとクラウドストレージの両方をサポートしています。

📖 **参照先:** まとめ/02_UI_Notebooks.md > セクション1（Snowsight）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Data」→「Add Data」をクリックする
2. 「Load files into a Stage」を選択する
3. ローカルのCSVファイルをドラッグ＆ドロップするか「Browse」でファイルを選択する
4. アップロード先のステージとデータベース/スキーマを指定してアップロードを完了する

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題13: Snowsightの Query History（クエリ履歴）のフィルタリング条件は？

Snowsightの Query History（クエリ履歴）画面では、どのような条件でクエリをフィルタリング（絞り込み）できますか？

- A) ネットワーク構成による絞り込み
- B) ハードウェア設定による絞り込み
- C) クエリのステータス、期間、およびユーザーによる絞り込み
- D) テキスト検索のみによる絞り込み

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: C) クエリのステータス、期間、およびユーザーによる絞り込み**

Query History画面では、様々な条件を組み合わせて過去のクエリを絞り込むことができます。主なフィルタリングオプションは以下の通りです。

- **ステータス（Status）**：実行時間が長いもの、失敗したもの、待機中のものなど。
    
- **ユーザー（User）**：すべてのユーザー、自分自身、または特定のユーザー（権限がある場合）。
    
- **期間（Time period）**：過去14日間以内の特定の日時。
    
- **その他のフィルター**：
    
    - **SQLテキスト（SQL Text）**：特定の構文（GROUP BYなど）を含むクエリ。
        
    - **クエリID（Query ID）**：特定のクエリの検索。
        
    - **ウェアハウス（Warehouse）**：実行に使用されたウェアハウス。
        
    - **ステートメントの種類（Statement Type）**：SELECT、INSERT、DELETEなど。
        
    - **実行時間（Duration）**：特定の長さ以上かかったクエリの特定。
        
    - **セッションID（Session ID） / クエリタグ（Query Tag）**など。
        

❌ ネットワーク構成やハードウェア設定による絞り込み機能はありません。また、テキスト検索「のみ」に限定されているわけでもありません。

📖 **参照先:** まとめ/02_UI_Notebooks.md > セクション1（Snowsight）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Monitoring」→「Query History」を開く
2. 上部のフィルターパネルで「Status」「Time」「User」「Warehouse」などのフィルター項目を確認する
3. 「Status」で「Failed」を選択して失敗したクエリだけを絞り込む
4. 特定のクエリをクリックして詳細（実行計画、エラー内容など）を確認する

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題14: Snowsightで新しいステージを作成する手順はどれですか？

Snowsightで新しいステージ（ファイルの置き場）を作成する手順はどれですか？

- A) Data >> Create Schema
- B) Data >> add data >> Create Schema
- C) Data Product >> Create Schema
- D) Data >> Database >> Schema >> Create Stage

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: D) Data >> Database >> Schema >> Create Stage**

Snowsightの画面操作で新しいステージを作成するには、オブジェクトの階層構造に従って移動します。

1. 「Data」タブを開く。
    
2. 対象の「Database（データベース）」を選択する。
    
3. その中の「Schema（スキーマ）」を選択する。
    
4. 右上の「+（作成）」ボタンをクリックし、「Create Stage」を選択する。
    

この画面から、内部ステージか外部ステージかの選択、暗号化の設定などを構成できます。

❌ その他の選択肢は、ステージではなくスキーマを作成するメニューを指していたり、存在しないメニュー階層であったりするため誤りです。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Data」→「Databases」を開く
2. 対象のデータベースを選択してスキーマの一覧を表示する
3. スキーマを選択し、右上の「Create」ボタン → 「Stage」をクリックする
4. Internal Stage か External Stage を選んで設定を入力し作成を完了する

**SQLでの確認:**
```sql
-- 内部ステージの作成
CREATE STAGE my_internal_stage;
-- 作成されたステージの確認
SHOW STAGES;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題15: 利用可能な「ビュー」の種類はどれですか？（3つ選択）

Snowflakeで利用可能な「ビュー」の種類はどれですか？（3つ選択）

- A) Materialized（マテリアライズドビュー）
- B) Non-Materialized（非マテリアライズドビュー / 通常のビュー）
- C) UnMaterialized
- D) Secure（セキュアビュー）

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) Materialized（マテリアライズドビュー）、B) Non-Materialized（非マテリアライズドビュー / 通常のビュー）、D) Secure（セキュアビュー）**

Snowflakeは以下の3種類のビューをサポートしています。

- **Non-Materialized Views（通常のビュー）**：SQLクエリによって定義される仮想的なテーブルです。検索されるたびに動的にクエリが実行され、結果が生成されます。
    
- **Materialized Views（マテリアライズドビュー）**：クエリの実行結果をあらかじめ計算し、物理的に保存しておくビューです。頻繁に検索される処理のパフォーマンスを向上させます。
    
- **Secure Views（セキュアビュー）**：裏側にあるクエリのロジック（計算式など）をユーザーから隠すためのビューです。機密データを共有する際などに使用されます。
    

❌ **UnMaterialized**：このような用語は存在しません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- 通常のビュー（Non-Materialized）の作成
CREATE VIEW my_view AS SELECT * FROM my_table WHERE status = 'active';

-- セキュアビューの作成（クエリ定義を非表示にする）
CREATE SECURE VIEW my_secure_view AS SELECT id, name FROM my_table;

-- マテリアライズドビューの作成（Enterprise Edition以上）
CREATE MATERIALIZED VIEW my_mat_view AS SELECT category, COUNT(*) AS cnt FROM my_table GROUP BY category;

-- ビューの一覧確認
SHOW VIEWS;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題16: Snowflakeの「データベース」の中に含めることができないものは？

Snowflakeの「データベース」の中に含めることができないものはどれですか？

- A) Views（ビュー）
- B) Tables（テーブル）
- C) Role（ロール）
- D) Database Role（データベースロール）

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: C) Role（ロール）**

Snowflakeにおいて、データベースは以下のオブジェクトを整理して格納するための論理的な入れ物です。

- **スキーマ**：その中にテーブル、ビュー、ステージ、関数などが含まれます。
    
- **データベースロール**：そのデータベース内でのみ有効な、きめ細かいアクセス制御のためのロールです。
    

しかし、一般的な**Role（ロール）**（SYSADMINやカスタムロールなど）は、データベースの中に存在するものではなく、「アカウント全体」のレベルで存在するオブジェクトです。そのため、データベースの中に含めることはできません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題17: Snowflakeの階層構造において、データベースとスキーマの関係は？（2つ選択）

Snowflakeの階層構造において、データベースとスキーマはどのように関係していますか？（2つ選択）

- A) 1つのデータベースの中に複数のスキーマを含めることができる。
- B) 1つのスキーマの中に複数のデータベースを含めることができる。
- C) 1つのスキーマの中に複数のテーブルやビューを含めることができる。
- D) 1つのテーブルの中に複数のスキーマを含めることができる。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) 1つのデータベースの中に複数のスキーマを含めることができる。、C) 1つのスキーマの中に複数のテーブルやビューを含めることができる。**

Snowflakeのデータ管理の階層は以下の順序で構成されています。

1. **データベース（Database）**：一番大きな入れ物。複数のスキーマを保持できます。
    
2. **スキーマ（Schema）**：テーブルやビューなどをまとめるグループ。
    
3. **テーブル・ビュー（Table / View）**：スキーマの中に格納されるオブジェクト。
    

❌ 「スキーマの中にデータベースが含まれる」や「テーブルの中にスキーマが含まれる」といった逆の階層構造は存在しません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題18: サポートしている「数値データ型」はどれですか？（2つ選択）

Snowflakeがサポートしている「数値データ型」はどれですか？（2つ選択）

- A) INTEGER
- B) BOOLEAN
- C) VARCHAR
- D) FLOAT

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) INTEGER、D) FLOAT**

Snowflakeは以下の主要な数値データ型をサポートしています。

- **INTEGER**：整数を扱います（内部的には `NUMBER(38,0)` として処理されます）。`INT`, `BIGINT`, `SMALLINT` などの別名も使用可能です。
    
- **FLOAT**：浮動小数点数を扱います。非常に幅広い範囲の数値を近似値として保存します。
    

❌ **BOOLEAN**：論理値（True / False）のデータ型です。

❌ **VARCHAR**：文字列（テキスト）のデータ型です。これらは数値データ型ではありません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題19: テーブルの種類として正しいものはどれですか？（3つ選択）

Snowflakeのテーブルの種類として正しいものはどれですか？（3つ選択）

- A) Permanent（永続テーブル）
- B) Temporary（一時テーブル）
- C) Transient（トランジェントテーブル）
- D) Virtual（仮想テーブル）

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) Permanent（永続テーブル）、B) Temporary（一時テーブル）、C) Transient（トランジェントテーブル）**

Snowflakeでは用途に応じて3種類のテーブルを作成できます。

- **Permanent（永続）**：デフォルトのテーブルです。長期間のデータ保存に使用され、過去のデータを保護する「Time Travel」（過去時点のデータにアクセスできる機能、最大90日）や障害復旧用の「Fail-safe」（Time Travel期間後の7日間の非常用リカバリ）機能が完全に適用されます。

- **Temporary（一時）**：接続しているセッション（作業中）の間だけ存在し、セッションが終了すると自動的に消去されるテーブルです。

- **Transient（トランジェント）**：永続テーブルに似ていますが、「Fail-safe（Time Travel期間後の7日間の非常用リカバリ機能）」の期間が設定されないテーブルです。再作成が容易なデータや、一時的な保管場所に適しており、コストを抑えることができます。
    

❌ **Virtual**：ビュー（View）のことを「仮想テーブル」と呼ぶことはありますが、テーブルの公式な種類（属性）としては存在しません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- Permanent テーブル（デフォルト）の作成
CREATE TABLE my_permanent_table (id INT, name VARCHAR(100));

-- Temporary テーブルの作成（セッション終了で自動削除）
CREATE TEMPORARY TABLE my_temp_table (id INT, data VARCHAR(200));

-- Transient テーブルの作成（Fail-safeなし・コスト削減向け）
CREATE TRANSIENT TABLE my_transient_table (id INT, value NUMBER);

-- テーブルの種類を確認
SHOW TABLES;
-- kind 列で PERMANENT / TEMPORARY / TRANSIENT の違いを確認する
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題20: Snowflakeを最も正確に表している説明はどれですか？

Snowflakeを最も正確に表している説明はどれですか？

- A) データウェアハウス・ソフトウェア
- B) ETLツール
- C) AIデータクラウドプラットフォーム
- D) トランザクションデータベース

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: C) AIデータクラウドプラットフォーム**

Snowflakeは、従来の「データウェアハウス（データを集めて分析する場所）」の枠組みを超えた、統合的な「AIデータクラウドプラットフォーム」として位置づけられています。

単なるデータの保存や分析だけでなく、組織間での安全なデータ共有、機械学習やAIワークロードへのネイティブ対応、StreamlitやSnowparkを用いたデータアプリの構築など、幅広い用途を単一の環境で提供しているためです。

❌ データウェアハウス機能も含まれていますが、それだけに限定した表現はプラットフォーム全体を正確に表していません。また、トランザクション（頻繁な書き込み）に特化したデータベースや、単なるデータ転送ツール（ETL）でもありません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題21: 新しいデータベースを作成するために使用されるSQLコマンドは？

Snowflakeで新しいデータベースを作成するために使用されるSQLコマンドはどれですか？

- A) `USE DATABASE database_name;`
- B) `CREATE DATABASE database_name;`
- C) `CREATE DB database_name;`
- D) `USE DB database_name;`

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) `CREATE DATABASE database_name;`**

新しいデータベースを作成するための正しい構文は `CREATE DATABASE <データベース名>;` です。これによって新しいデータの入れ物が作成され、データを整理して保存できるようになります。

❌ **`USE DATABASE`**：既存のデータベースを「これ以降の作業対象にする」と指定するコマンドであり、作成はしません。

❌ **`CREATE DB` / `USE DB`**：`DB` という省略形はSnowflakeの構文としてサポートされていません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- データベースの作成
CREATE DATABASE my_test_db;

-- 作成されたデータベースを確認
SHOW DATABASES;

-- 使用するデータベースを指定
USE DATABASE my_test_db;

-- スキーマ・テーブルの作成へと続ける
CREATE SCHEMA my_schema;
CREATE TABLE my_schema.my_table (id INT, name VARCHAR(100));
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題22: ワークシートで `INSERT` コマンドを実行する前に必要なものは？

Snowflakeのワークシートで `INSERT`（データ挿入）コマンドを実行する前に必要なものはどれですか？

- A) アクティブなウェアハウス
- B) データベース
- C) スキーマ
- D) それらすべて

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: D) それらすべて**

テーブルにデータを挿入（`INSERT`）するには、以下の3つの要素がすべて揃っている必要があります。

1. **アクティブなウェアハウス**：処理を実行するための計算リソース（コンピューター）。
    
2. **データベース**：テーブルが保存されている大きな枠組み。
    
3. **スキーマ**：データベース内でテーブルを整理しているフォルダのような枠組み。
    

システムは「どの計算リソースを使って、どこに書き込むか」を知る必要があるため、どれか1つでも欠けているとコマンドは実行できません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- ウェアハウス・DB・スキーマをすべて指定してから INSERT を実行
USE WAREHOUSE my_warehouse;
USE DATABASE my_db;
USE SCHEMA my_schema;

-- テーブルへのデータ挿入
INSERT INTO my_table (id, name) VALUES (1, 'テストデータ');

-- ウェアハウスなしで実行するとエラーになることを確認（ウェアハウスを停止後に試す）
ALTER WAREHOUSE my_warehouse SUSPEND;
INSERT INTO my_table (id, name) VALUES (2, 'エラーテスト'); -- エラーが発生する
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題23: RBAC（ロールベースアクセス制御）システムの主要なコンポーネントは？（3つ選択）

SnowflakeのRBAC（ロールベースアクセス制御）システムの主要なコンポーネント（構成要素）はどれですか？（3つ選択）

- A) Permissions（許可）
- B) Roles（ロール）
- C) Privileges（権限）
- D) Users（ユーザー）

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) Roles（ロール）、C) Privileges（権限）、D) Users（ユーザー）**

RBACとは「ロールベースアクセス制御（Role-Based Access Control）」の略で、ロールに権限を割り当て、ユーザーにロールを付与するという仕組みでアクセスを管理します。Snowflakeのアクセス制御は、以下の要素の組み合わせで機能します。

- **Users（ユーザー）**：システムを利用する人やサービスです。

- **Roles（ロール）**：ユーザーに割り当てられる「役割」です。アクセス制御の要となります。

- **Privileges（権限）**：テーブルを見られる（SELECT）、データを追加できる（INSERT）といった、オブジェクトに対する具体的な操作許可のことです。


ユーザーに直接権限を与えるのではなく、「権限を持ったロール」を「ユーザー」に与えることで管理を効率化しています。

❌ **Permissions**：「Permissions」という言葉は一般的な用語として使われますが、SnowflakeのRBACを構成する厳密なコンポーネント名は「Privileges（権限）」です。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Admin」→「Users & Roles」を開く
2. 「Roles」タブでシステム定義ロール（ACCOUNTADMIN、SYSADMIN 等）と付与された権限を確認する
3. 「Users」タブでユーザーに割り当てられたロールを確認する

**SQLでの確認:**
```sql
-- ロール一覧の確認
SHOW ROLES;

-- ユーザーに付与されたロールの確認
SHOW GRANTS TO USER <username>;

-- ロールに付与された権限の確認
SHOW GRANTS TO ROLE SYSADMIN;

-- ユーザーの一覧確認
SHOW USERS;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題24: Snowflakeにおける「ロールの階層化」の目的は何ですか？

Snowflakeにおける「ロールの階層化」の目的は何ですか？

- A) SQLクエリの実行順序を定義するため。
- B) ロールを整理し、権限を継承（引き継ぎ）させるため。
- C) ネットワークの設定を管理するため。
- D) 生データを保存するため。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) ロールを整理し、権限を継承（引き継ぎ）させるため。**

Snowflakeでは、あるロールを別のロールに与えることで「階層構造」を作ることができます。これにより、上位のロールが下位のロールの持っている権限を自動的に「継承（引き継ぎ）」できるようになります。

例えば、「マネージャー」ロールの下に「アナリスト」ロールを配置すると、マネージャーはアナリストが持つすべてのアクセス権を自動的に得ることができます。これにより、権限の付与作業を最小限に抑え、セキュリティ管理を簡素化できます。

❌ SQLクエリの実行順序、ネットワーク管理、データの保存とは無関係の仕組みです。

📖 **参照先:** まとめ/05_アクセス制御_ロール.md > セクション1（RBAC）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- ロールの階層（継承）を設定する
CREATE ROLE analyst_role;
CREATE ROLE manager_role;

-- manager_role に analyst_role を付与（階層化）
GRANT ROLE analyst_role TO ROLE manager_role;

-- ロールの継承関係を確認
SHOW GRANTS TO ROLE manager_role;
-- manager_role が analyst_role を含んでいることが確認できる
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題25: Snowflakeにおける「カスタムロール」の主な特徴は何ですか？（2つ選択）

Snowflakeにおける「カスタムロール」の主な特徴は何ですか？（2つ選択）

- A) ユーザーに付与することはできない。
- B) 組織の特定の要件に合わせてカスタマイズできる。
- C) あらかじめ定義された権限を持っている。
- D) Snowflakeの管理者によって作成される。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) 組織の特定の要件に合わせてカスタマイズできる。、D) Snowflakeの管理者によって作成される。**

標準で用意されているロール（SYSADMINなど）とは別に、管理者は組織のルール（例：「経理部用」「開発チーム用」など）に合わせた独自の「カスタムロール」を作成できます。これにより、必要な権限だけをきめ細かく付与し、過剰なアクセス権を与えないように制御できます。

❌ **ユーザーに付与することはできない**：カスタムロールも通常のロールと同様にユーザーに付与して使用します。

❌ **あらかじめ定義された権限を持っている**：作成時点では権限を持っていません。管理者が明示的に権限を付与する必要があります。

📖 **参照先:** まとめ/04_アカウント_セキュリティ.md > セクション1（アカウント管理）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- カスタムロールの作成（作成直後は権限なし）
CREATE ROLE sales_analyst;

-- 作成直後の権限確認（権限が空であることを確認）
SHOW GRANTS TO ROLE sales_analyst;

-- 管理者が明示的に権限を付与する
GRANT USAGE ON DATABASE sales_db TO ROLE sales_analyst;
GRANT USAGE ON SCHEMA sales_db.public TO ROLE sales_analyst;
GRANT SELECT ON TABLE sales_db.public.orders TO ROLE sales_analyst;

-- 付与後の権限確認
SHOW GRANTS TO ROLE sales_analyst;

-- ユーザーにカスタムロールを付与
GRANT ROLE sales_analyst TO USER my_user;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題26: Snowflakeにおいて、ロールはどのようにしてオブジェクトにアクセスできますか？（2つ選択）

Snowflakeにおいて、ロールはどのようにしてオブジェクト（テーブルなど）にアクセスできますか？（2つ選択）

- A) オブジェクトに対する権限を直接割り当てられることによって。
- B) 下位のロールから権限を継承することによって。
- C) ネットワーク設定を構成することによって。
- D) 生データを保存することによって。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) オブジェクトに対する権限を直接割り当てられることによって。、B) 下位のロールから権限を継承することによって。**

ロールがテーブルやビューなどのオブジェクトを操作できるようになるには、以下の2つの経路があります。

1. **直接の割り当て**：そのロールに対して直接「このテーブルを見てよい（SELECT）」という権限が与えられる。

2. **継承（下位ロールからの引き継ぎ）**：権限を持っている別のロール（下位ロール）を自分の配下に取り込む（階層化する）ことで、その権限を自動的に引き継ぐ。例えば「マネージャー」ロールの下に「アナリスト」ロールを配置すると、マネージャーはアナリストが持つすべての権限を自動的に継承します。重要なのは「**上位ロールが下位ロールの権限を継承する**」という方向性です。

❌ ネットワークの設定やデータの保存行為によってアクセス権限が発生するわけではありません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- 直接の権限付与：ロールに対してテーブルの SELECT 権限を付与
GRANT SELECT ON TABLE my_db.my_schema.orders TO ROLE analyst_role;

-- 階層による継承：manager_role の下に analyst_role を配置
GRANT ROLE analyst_role TO ROLE manager_role;
-- → manager_role は analyst_role の持つ SELECT 権限を自動的に継承する

-- 権限の確認
SHOW GRANTS TO ROLE analyst_role;
SHOW GRANTS TO ROLE manager_role;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題27: Snowflakeで、ロールに権限を付与するために使用されるコマンドは？

Snowflakeで、ロールに権限を付与（許可）するために使用されるコマンドはどれですか？

- A) ASSIGN
- B) CREATE
- C) REVOKE
- D) GRANT

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: D) GRANT**

`GRANT` コマンドは、特定のロールに対して、データベースやテーブルといったオブジェクトの操作権限（SELECT、INSERTなど）を付与するためのSQLコマンドです。

❌ **REVOKE**：付与した権限を取り消す（剥奪する）ためのコマンドです。

❌ **CREATE**：オブジェクトを新規作成するためのコマンドです。

❌ **ASSIGN**：SQLの権限管理コマンドとしては存在しません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- GRANT で権限を付与する
GRANT SELECT ON TABLE my_table TO ROLE analyst_role;
GRANT USAGE ON WAREHOUSE my_wh TO ROLE analyst_role;

-- 付与された権限を確認
SHOW GRANTS TO ROLE analyst_role;

-- REVOKE で権限を取り消す（対比として確認）
REVOKE SELECT ON TABLE my_table FROM ROLE analyst_role;

-- 取り消し後の権限確認
SHOW GRANTS TO ROLE analyst_role;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題28: Snowflakeのクエリにおける `LIMIT` 句の主な目的は何ですか？

Snowflakeのクエリにおける `LIMIT` 句の主な目的は何ですか？

- A) 結果の並べ替えを行うため。
- B) 結果の条件絞り込みを行うため。
- C) 返される行数を制限するため。
- D) 結果のグループ化を行うため。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: C) 返される行数を制限するため。**

`LIMIT` 句は、クエリが検索結果として出力する「最大の行数」を制限するために使用されます。大量のデータからサンプルとして数行だけを確認したい場合や、パフォーマンスを最適化したい場合に役立ちます。通常、上位のデータを抽出するために `ORDER BY`（並べ替え）句と組み合わせて使用されます。

❌ **結果の並べ替え**：`ORDER BY` 句を使用します。

❌ **結果の条件絞り込み**：`WHERE` 句を使用します。

❌ **結果のグループ化**：`GROUP BY` 句を使用します。

📖 **参照先:** まとめ/05_アクセス制御_ロール.md

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題29: `SELECT` クエリの検索対象から特定の列だけを除外するにはどうすればよいですか？

`SELECT` クエリの検索対象から特定の列（カラム）だけを除外したい場合、どのようにすべきですか？

- A) 含めたい列をすべて明示的に記述する。
- B) EXCLUDE キーワードを使用する。
- C) テーブルに対して REMOVE 関数を使用する。
- D) OMIT 句を使用する。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) EXCLUDE キーワードを使用する。**

Snowflakeでは、`SELECT * EXCLUDE (列名)` という構文を使用することで、すべての列（`*`）の中から「指定した特定の列だけを除外」して検索結果を取得できます。

列の数が多いテーブルで、数個の列だけを省きたい場合に、含める列をすべて手書きする手間を省き、コードを簡潔に保つことができます。

❌ **含めたい列をすべて明示的に記述する**：可能ですが、非常に手間がかかり保守性が下がります。

❌ **REMOVE 関数 / OMIT 句**：Snowflakeにはこのような関数や句は存在しません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題30: Snowflakeにおける「完全修飾名（Fully Qualified Name）」の用途は？

Snowflakeにおける「完全修飾名（Fully Qualified Name）」は何のために使用されますか？

- A) データベースオブジェクトを一意に（重複なく）特定するため。
- B) テーブルにデータを保存するため。
- C) ユーザーの権限を管理するため。
- D) ネットワーク設定を構成するため。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) データベースオブジェクトを一意に（重複なく）特定するため。**

完全修飾名とは、`<データベース名>.<スキーマ名>.<オブジェクト名>` のように、所属する階層をすべて記述した名前のことです。複数のデータベースやスキーマに同じ名前のテーブルが存在する場合でも、この形式を使用することで、システムが「どのテーブルを指しているか」を正確に特定できるようになります。

❌ データの保存、権限管理、ネットワーク設定とは無関係の概念です。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題31: `DROP SCHEMA IF EXISTS marketing_schema;` を実行するとどうなりますか？

Snowflakeで `DROP SCHEMA IF EXISTS marketing_schema;` というコマンドを実行するとどうなりますか？

- A) スキーマが存在しない場合、エラーが返される。
- B) "marketing_schema" というスキーマが存在しない場合、作成される。
- C) "marketing_schema" というスキーマが「存在する場合にのみ」削除される。
- D) "marketing_schema" スキーマにテーブルが含まれていても、強制的に削除される。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: C) "marketing_schema" というスキーマが「存在する場合にのみ」削除される。**

`IF EXISTS` という条件をつけることで、「もし対象のスキーマが存在していれば削除するが、存在しなくてもエラーにはせず、処理を正常終了として通過させる」という安全な動きになります。

❌ **エラーが返される**：`IF EXISTS` があるため、エラーにはなりません。

❌ **作成される**：`DROP` は削除コマンドであり、作成はしません。

❌ **テーブルが含まれていても強制削除される**：この説明自体は `DROP SCHEMA` の動作として一部正しいですが、コマンドの「主目的・全体の結果」を正確に表す選択肢としては不十分です。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題32: Snowflakeにおける `PUBLIC` スキーマの目的は何ですか？（2つ選択）

Snowflakeにおける `PUBLIC` スキーマの目的は何ですか？（2つ選択）

- A) ユーザー個人のデータを保存するため。
- B) 新しいオブジェクトを作成する際の、デフォルト（初期状態）のスキーマを提供するため。
- C) アカウントレベルのメタデータ（システム情報）を保存するスキーマ。
- D) すべてのユーザーがアクセスできる共有リソースを保存するため。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) 新しいオブジェクトを作成する際の、デフォルト（初期状態）のスキーマを提供するため。、D) すべてのユーザーがアクセスできる共有リソースを保存するため。**

データベースを作成すると、自動的に `PUBLIC` という名前のスキーマが作成されます。

- 特に指定せずにテーブルを作成した場合、この `PUBLIC` スキーマの中に保存されます。
    
- デフォルトでは、適切な権限を持つすべてのユーザーがアクセスできるため、組織内で共通して使うデータなどの保管場所として利用されます。
    

❌ **ユーザー個人のデータ**：個人用の専用スキーマを作成するのが一般的です。

❌ **アカウントレベルのメタデータ**：これは `INFORMATION_SCHEMA` という別のシステムスキーマの役割です。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題33: Snowflakeの「Cloning（クローン）」機能の主な目的は何ですか？

Snowflakeの「Cloning（クローン）」機能の主な目的は何ですか？

- A) データをリアルタイムでバックアップするため。
- B) ストレージ容量を二重に消費することなく、データベース、スキーマ、またはテーブルの完全なコピー（複製）を作成するため。
- C) ユーザーの権限を管理するため。
- D) Snowflakeアカウント全体のバックアップを作成するため。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) ストレージ容量を二重に消費することなく、データベース、スキーマ、またはテーブルの完全なコピー（複製）を作成するため。**

Snowflakeのクローン機能は「ゼロコピークローン」と呼ばれます。元データのポインタ（参照先の情報）をコピーするだけで、新たなストレージ領域を使わずに複製を作れる仕組みです。作成直後は元のデータと同じストレージ領域を共有しているため、余分なストレージコストをかけずに一瞬でコピーを作成できます。クローン後に変更が加えられた部分だけが追加のストレージを消費します。テスト環境の構築や開発作業などに非常に有効です。

❌ **リアルタイムのバックアップ**：過去のデータを守る目的には「Time Travel」機能を使用します。

❌ **アカウント全体のバックアップ**：クローンはオブジェクト単位（データベースやテーブル）で行う機能です。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- テーブルのゼロコピークローンを作成
CREATE TABLE orders_clone CLONE orders;

-- 元テーブルとクローンのデータ件数が同じことを確認
SELECT COUNT(*) FROM orders;
SELECT COUNT(*) FROM orders_clone;

-- クローンに変更を加えても元テーブルには影響しない
INSERT INTO orders_clone VALUES (99999, '2026-01-01', 'test');
SELECT COUNT(*) FROM orders;        -- 変わらない
SELECT COUNT(*) FROM orders_clone;  -- 1件増えている

-- データベース全体のクローン
CREATE DATABASE my_db_clone CLONE my_db;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題34: Snowflakeのクローン機能でコピーできるものはどれですか？（3つ選択）

Snowflakeのクローン機能を使用してコピー（複製）できるものはどれですか？（3つ選択）

- A) Databases（データベース）
- B) Schemas（スキーマ）
- C) Roles（ロール）
- D) Tables（テーブル）

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) Databases（データベース）、B) Schemas（スキーマ）、D) Tables（テーブル）**

Snowflakeのクローン機能は、データを保持するコンテナやオブジェクトに対して適用できます。

- データベース全体（中のスキーマやテーブルも含めて）。
    
- スキーマ全体。
    
- 個別のテーブル。
    

❌ **Roles（ロール）**：ロールはデータを保持するオブジェクトではなく、「誰が何にアクセスできるか」を定義する権限管理のオブジェクトです。クローンはデータを複製する機能であるため、アクセス権限の設定であるロールはコピー対象外となります。手動で作成・設定する必要があります。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- データベース・スキーマ・テーブルのクローンはすべて可能
CREATE DATABASE dev_db CLONE prod_db;
CREATE SCHEMA dev_schema CLONE prod_schema;
CREATE TABLE dev_orders CLONE prod_orders;

-- ロールのクローンは不可（エラーになることを確認）
-- CREATE ROLE analyst_role_clone CLONE analyst_role; -- このような構文は存在しない

-- クローンされたオブジェクトの確認
SHOW DATABASES LIKE 'dev_%';
SHOW TABLES LIKE 'dev_%';
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題35: Standard EditionのTime Travelデータ保持の最大期間は何日ですか？

Standard Edition（標準アカウント）のSnowflakeにおいて、「Time Travel（タイムトラベル）」機能で過去のデータを保持できる最大期間は何日ですか？

- A) 0
- B) 1
- C) 7
- D) 90

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) 1**

Snowflake Standard Edition では、Time Travel（過去の時点のデータをSELECTで参照したり、誤って削除・更新したデータを復元したりできる機能）のデータ保持期間は最大 **1日（24時間）** です。

※設定で 0日 にして機能を無効にすることも可能です。

❌ **90日**：最大90日間の保持が可能なのは、Enterprise Edition（エンタープライズ版）以上のアカウントです。Standard Edition では、どれだけ設定を変更しても1日が上限となります。

❌ **7日**：7日間はFail-safe（Time Travel期間終了後の緊急リカバリ期間）の日数です。Time Travelの最大期間ではありません。

📖 **参照先:** まとめ/07_データ保護_共有.md > セクション1（Time Travel）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- 現在時刻を記録
SELECT CURRENT_TIMESTAMP();

-- テーブルのデータを変更
DELETE FROM my_table WHERE id = 1;

-- 過去60秒前の状態を参照（Time Travel）
SELECT * FROM my_table AT(OFFSET => -60) LIMIT 10;

-- テーブルの DATA_RETENTION_TIME_IN_DAYS 設定を確認
SHOW TABLES LIKE 'my_table';
-- DATA_RETENTION_TIME 列で保持期間を確認

-- テーブルの保持期間を変更
ALTER TABLE my_table SET DATA_RETENTION_TIME_IN_DAYS = 1;

-- 誤って削除したテーブルを復元
DROP TABLE my_table;
UNDROP TABLE my_table;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題36: Marketplace や Data Exchange における「プロバイダー」とは？

Snowflake Marketplace や Data Exchange の文脈において、「プロバイダー（Provider）」とは何を指しますか？

- A) データを消費（利用）するだけのユーザー。
- B) Snowflakeのインフラストラクチャを管理するユーザー。
- C) データ製品を共有（提供）するユーザー、または組織。
- D) Snowflake社の従業員であるユーザー。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: C) データ製品を共有（提供）するユーザー、または組織。**

プロバイダーとは、自分が所有しているデータセットやアプリケーションを、マーケットプレイスなどを通じて他のSnowflakeユーザーに提供する側の人や組織のことです。

❌ **データを消費するユーザー**：これは「コンシューマー（Consumer）」と呼ばれます。

❌ インフラの管理者や、Snowflakeの社員を指す言葉ではありません。

📖 **参照先:** まとめ/02_UI_Notebooks.md > セクション9（Marketplace）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Data Products」→「Marketplace」を開く
2. 検索バーでデータを検索し、各リスティングの「Provider」情報（提供者名）を確認する
3. 「Get」ボタンからデータセットを取得（コンシューマーとして利用）する操作を確認する
4. 自分がプロバイダーになる場合は「Provider Studio」から出品操作を行う

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題37: Marketplace における「データリスティング」に関する正しい記述は？

Snowflake Marketplace における「データリスティング（データの出品・掲載）」に関する記述として正しいものはどれですか？

- A) アクセスするためには必ず支払いが必要である。
- B) 静的なデータ（過去のある時点でのスナップショット）に限定されている。
- C) プロバイダーの条件に応じて、無料の場合もあれば有料の場合もある。
- D) Snowflakeの管理者のみがアクセスできる。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: C) プロバイダーの条件に応じて、無料の場合もあれば有料の場合もある。**

マーケットプレイスに出品されているデータセットは、プロバイダー（提供者）の意向によって料金体系が異なります。完全に無料で公開されているデータセットもあれば、サブスクリプションなどの有料モデルで提供されているものもあります。

❌ **必ず支払いが必要である**：無料のデータも多数存在します。

❌ **静的なデータに限定される**：常に最新のデータが自動で反映されるライブデータも提供可能です。

❌ **管理者のみアクセスできる**：適切な権限を与えられた一般ユーザーもアクセス可能です。

📖 **参照先:** まとめ/02_UI_Notebooks.md > セクション9（Marketplace）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Data Products」→「Marketplace」を開く
2. 無料のデータセット（例：天気データや経済統計など）を検索する
3. リスティングの詳細ページで「Free」または「Paid」の表示を確認する
4. 無料のデータセットを「Get」して自アカウントで利用できることを確認する

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題38: Data Exchange における「プライベートリスティング」と「パブリックリスティング」の違いは？

Snowflake Data Exchange における「プライベートリスティング（非公開）」と「パブリックリスティング（公開）」の決定的な違いは何ですか？

- A) パブリックリスティングは有料であり、プライベートリスティングは無料である。
- B) パブリックリスティングはSnowflake社員のみがアクセスでき、プライベートリスティングは外部ユーザー向けである。
- C) プライベートリスティングは特定のSnowflakeアカウントに限定して共有され、パブリックリスティングはすべてのSnowflakeユーザーが利用できる。
- D) プライベートリスティングはコードの共有用であり、パブリックリスティングはデータの共有用である。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: C) プライベートリスティングは特定のSnowflakeアカウントに限定して共有され、パブリックリスティングはすべてのSnowflakeユーザーが利用できる。**

- **プライベートリスティング**：特定の取引先や社内の別アカウントなど、提供者が指定したアカウントに対してのみデータを共有する安全な方法です。
    
- **パブリックリスティング**：Snowflake Marketplace を通じて、Snowflakeを利用しているすべてのユーザーに向けて広く公開される形式です。
    

❌ 無料か有料か、あるいはコードかデータかといった違いではありません。

📖 **参照先:** まとめ/02_UI_Notebooks.md > セクション9（Marketplace）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Data Products」→「Private Sharing」を開く
2. 「Share」ボタンから新しい共有を作成する
3. 共有先として特定のSnowflakeアカウントIDを指定する（プライベートリスティングの確認）
4. 対比として「Marketplace」→「Provider Studio」でパブリック公開のオプションを確認する

**SQLでの確認:**
```sql
-- プライベート共有の作成
CREATE SHARE my_private_share;
GRANT USAGE ON DATABASE my_db TO SHARE my_private_share;
GRANT USAGE ON SCHEMA my_db.public TO SHARE my_private_share;
GRANT SELECT ON TABLE my_db.public.my_table TO SHARE my_private_share;

-- 特定アカウントにのみ共有
ALTER SHARE my_private_share ADD ACCOUNTS = <account_identifier>;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題39: Snowflake Data Exchange 機能の初期有効化は通常どのように行われますか？

プライベートなデータ共有環境を構築するために必要な「Snowflake Data Exchange 機能の初期有効化」は、通常どのように行われますか？

- A) Snowflakeのアカウント管理者であれば、誰でもWeb画面からワンクリックで有効化できる。
- B) 通常、Snowflakeサポートへのリクエストが必要であり、サポート側が特定のアカウントに対して有効化処理を行う。
- C) Data Exchangeは、アカウントが作成された時点で自動的にすべてのユーザーに対して有効化されている。
- D) `ACCOUNTADMIN` ロールが特定のSQLコマンドを実行することで有効化される。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) 通常、Snowflakeサポートへのリクエストが必要であり、サポート側が特定のアカウントに対して有効化処理を行う。**

Snowflake Data Exchange（組織独自のプライベートなデータ共有ハブ）は、デフォルトで有効になっている機能ではありません。利用を開始するには、Snowflakeのサポート窓口に連絡して機能の有効化をリクエストする必要があります。

❌ 管理者画面からのワンクリック操作や、特定のSQLコマンドでは有効化できません。

📖 **参照先:** まとめ/04_アカウント_セキュリティ.md > セクション1（アカウント管理）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題40: Snowflakeにおけるウェアハウスの「スケールイン / スケールアウト」とは？

Snowflakeにおけるウェアハウスの「スケールイン / スケールアウト」とは何を意味しますか？

- A) マルチクラスターウェアハウスにおいて、クラスターの数を増減させること。
- B) ウェアハウスのサイズ（例：SmallからMediumへ、またはその逆）を変更すること。
- C) データを異なる保存場所間で移動させること。
- D) ウェアハウスのリージョン（地域）を変更すること。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) マルチクラスターウェアハウスにおいて、クラスターの数を増減させること。**

「スケールアウト」は、同時アクセスが増えたときに処理能力の単位である「クラスター」の数を追加して並行処理能力を高めることを指します。「スケールイン」は、アクセスが減ったときにクラスター数を減らして元に戻すことを指します。

❌ **ウェアハウスのサイズを変更する**：これは「スケールアップ / スケールダウン」と呼ばれます。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション3（Virtual Warehouse）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Admin」→「Warehouses」を開く
2. マルチクラスターウェアハウスを選択し、「Min Clusters」「Max Clusters」の設定を確認する
3. スケールアウト（クラスター数増加）とスケールアップ（サイズ変更）の設定箇所の違いを確認する

**SQLでの確認:**
```sql
-- マルチクラスターウェアハウスの作成（Enterprise Edition以上）
CREATE WAREHOUSE mc_warehouse
    WITH WAREHOUSE_SIZE = 'MEDIUM'
    MIN_CLUSTER_COUNT = 1
    MAX_CLUSTER_COUNT = 3
    SCALING_POLICY = 'STANDARD';

-- ウェアハウスの詳細確認
SHOW WAREHOUSES LIKE 'mc_warehouse';
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題41: 「マルチクラスターウェアハウス」を使用する利点はどれですか？（3つ選択）

Snowflakeで「マルチクラスターウェアハウス」を使用する利点はどれですか？（3つ選択）

- A) ピーク時（高負荷時）におけるクエリのパフォーマンスが向上する。
- B) クエリの需要（負荷）に基づいて、自動的にクラスターの数が調整（スケーリング）される。
- C) ストレージコストが削減される。
- D) 全体としての計算能力（コンピュート容量）が増加する。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) ピーク時（高負荷時）におけるクエリのパフォーマンスが向上する。、B) クエリの需要（負荷）に基づいて、自動的にクラスターの数が調整（スケーリング）される。、D) 全体としての計算能力（コンピュート容量）が増加する。**

マルチクラスターウェアハウスは、大量のクエリが同時に実行された際に、自動的に計算ノード群（クラスター）を追加稼働させます。これにより、クエリの待ち時間（キューイング）を解消し、システム全体の処理能力を高め、負荷が下がれば自動でクラスターを減らしてコストを最適化します。

❌ **ストレージコストが削減される**：マルチクラスターウェアハウスは「計算処理能力」に関する機能であり、データを保存するストレージ容量やそのコストには影響しません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション3（Virtual Warehouse）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Admin」→「Warehouses」を開く
2. ウェアハウスの状態（Started / Suspended）を確認する
3. 「...」メニューから「Suspend」「Resume」「Edit」を操作する
4. Monitoring でウェアハウスのクエリ実行数・負荷を確認する

**SQLでの確認:**
```sql
-- マルチクラスターウェアハウスの確認
SHOW WAREHOUSES;
-- CLUSTER_COUNT 列でアクティブなクラスター数を確認

-- ウェアハウスの負荷テスト（複数クエリを並行実行して自動スケールを観察）
SELECT COUNT(*) FROM large_table;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題42: Snowflakeで新しい「仮想ウェアハウス」を作成するためのコマンドは？

Snowflakeで新しい「仮想ウェアハウス」を作成するためのコマンドはどれですか？

- A) `CREATE VIRTUALWAREHOUSE warehouse_name;`
- B) `CREATE DATABASE warehouse_name;`
- C) `CREATE SCHEMA warehouse_name;`
- D) `CREATE WAREHOUSE warehouse_name;`

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: D) `CREATE WAREHOUSE warehouse_name;`**

仮想ウェアハウス（計算リソース）を新規作成する正しいSQLコマンドは `CREATE WAREHOUSE` です。このコマンドの実行時に、ウェアハウスのサイズや自動停止などの設定項目を指定できます。

❌ **`CREATE VIRTUALWAREHOUSE`**：`VIRTUAL` という単語はコマンド内には含めません。

❌ **`CREATE DATABASE / SCHEMA`**：これらはデータベースやスキーマを作成するコマンドです。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション3（Virtual Warehouse）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Admin」→「Warehouses」→「+ Warehouse」をクリックする
2. ウェアハウス名、サイズ（X-Small / Small / Medium など）を選択して作成する

**SQLでの確認:**
```sql
-- 仮想ウェアハウスの作成
CREATE WAREHOUSE my_new_wh
    WITH WAREHOUSE_SIZE = 'XSMALL'
    AUTO_SUSPEND = 60
    AUTO_RESUME = TRUE
    COMMENT = 'テスト用ウェアハウス';

-- 作成されたウェアハウスの確認
SHOW WAREHOUSES;

-- ウェアハウスの起動・停止
ALTER WAREHOUSE my_new_wh RESUME;
ALTER WAREHOUSE my_new_wh SUSPEND;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題43: 仮想ウェアハウスにおける「自動停止（Auto-suspend）」機能の目的は？

仮想ウェアハウスにおける「自動停止（Auto-suspend）」機能の目的は何ですか？

- A) ピーク時にウェアハウスのサイズを自動的に大きくするため。
- B) 非アクティブ（利用されていない）状態が一定期間続いた後に、自動的にウェアハウスを停止させるため。
- C) ウェアハウスにノードを自動的に追加するため。
- D) クラスターの数を自動的に減らすため。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) 非アクティブ（利用されていない）状態が一定期間続いた後に、自動的にウェアハウスを停止させるため。**

クエリが実行されずアイドル状態が続いた場合に、指定した時間（例：5分後）が経過するとウェアハウスを自動的にスリープ状態にする機能です。Snowflakeはウェアハウスが稼働している秒数単位で課金されるため、この機能によって無駄な計算コストの発生を防ぐことができます。

❌ サイズの変更や、クラスターの増減（スケーリング）を行う機能ではありません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション3（Virtual Warehouse）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Admin」→「Warehouses」で対象ウェアハウスを選択する
2. 「Edit」をクリックして「Auto Suspend」の設定時間（秒単位）を確認する
3. 設定を 60 秒に変更して保存し、アイドル状態で自動停止されることを観察する

**SQLでの確認:**
```sql
-- Auto-suspend の設定（60秒後に自動停止）
ALTER WAREHOUSE my_wh SET AUTO_SUSPEND = 60;

-- Auto-resume の有効化（クエリ実行時に自動起動）
ALTER WAREHOUSE my_wh SET AUTO_RESUME = TRUE;

-- 設定内容の確認
SHOW WAREHOUSES LIKE 'my_wh';
-- AUTO_SUSPEND と AUTO_RESUME 列を確認する
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題44: マルチクラスターウェアハウスにおける「オートスケール（Auto-scale）」機能に関する正しい記述は？（2つ選択）

マルチクラスターウェアハウスにおける「オートスケール（Auto-scale）」機能に関する記述として正しいものはどれですか？（2つ選択）

- A) クエリの需要（負荷）に基づいて、クラスターの数を自動的に調整する。
- B) クラスターを追加または削除するには手動での介入が必要である。
- C) 変動するワークロード（作業量）において、リソースの利用効率を最適化するのに役立つ。
- D) 標準（Standard）ウェアハウスでのみ利用可能である。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) クエリの需要（負荷）に基づいて、クラスターの数を自動的に調整する。、C) 変動するワークロード（作業量）において、リソースの利用効率を最適化するのに役立つ。**

オートスケール機能は、システムが負荷状況を監視し、混雑してきたらクラスターを増やし、空いてきたら減らすという調整をすべて自動で行います。これにより、パフォーマンスを維持しながら無駄なコストを抑えることができます。

❌ **手動での介入が必要**：すべて自動で処理されます。

❌ **標準（Standard）ウェアハウスでのみ利用可能**：逆です。マルチクラスターウェアハウスおよびオートスケール機能は **Enterprise Edition（エンタープライズ版）以上** のアカウントで利用できる機能です。Standard Edition では単一クラスターのウェアハウスしか利用できません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション3（Virtual Warehouse）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Admin」→「Warehouses」で対象ウェアハウスの「Edit」を開く
2. 「Max Clusters」と「Min Clusters」の設定欄でオートスケールの範囲を確認・変更する
3. 「Scaling Policy」（Standard / Economy）の違いを確認する

**SQLでの確認:**
```sql
-- オートスケールの設定確認
SHOW WAREHOUSES;
-- MIN_CLUSTER_COUNT / MAX_CLUSTER_COUNT / SCALING_POLICY 列を確認

-- オートスケール範囲の変更
ALTER WAREHOUSE my_wh SET
    MIN_CLUSTER_COUNT = 1
    MAX_CLUSTER_COUNT = 5
    SCALING_POLICY = 'STANDARD';
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題45: Snowflakeで利用できる「ステージ」の種類はどれですか？（2つ選択）

Snowflakeで利用できる「ステージ（データの置き場）」の種類はどれですか？（2つ選択）

- A) Internal（内部）
- B) External（外部）
- C) Public（パブリック）
- D) Private（プライベート）

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) Internal（内部）、B) External（外部）**

Snowflakeには、データをテーブルにロードする前の一時置き場として、2種類のステージが用意されています。

- **内部ステージ（Internal Stage）**：Snowflakeのシステム内部に用意されたストレージ領域。
    
- **外部ステージ（External Stage）**：AWS S3、Azure Blob Storage、Google Cloud Storageなどの、ユーザー自身が管理している外部のクラウドストレージ領域。
    

❌ **Public / Private**：ステージの公式な種類としては存在しません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- 内部ステージ（ユーザーステージ）の確認
LIST @~;  -- 自分のユーザーステージ

-- 内部ステージ（名前付き）の作成と確認
CREATE STAGE my_internal_stage;
SHOW STAGES;

-- 外部ステージ（S3の例）の作成
CREATE STAGE my_s3_stage
    URL = 's3://my-bucket/data/'
    CREDENTIALS = (AWS_KEY_ID = '...' AWS_SECRET_KEY = '...');

-- ステージ内のファイル一覧
LIST @my_internal_stage;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題46: 「半構造化データ」のフォーマット（形式）の例はどれですか？（3つ選択）

「半構造化データ」のフォーマット（形式）の例はどれですか？（3つ選択）

- A) JSON
- B) CSV
- C) Parquet
- D) XML

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) JSON、C) Parquet、D) XML**

「半構造化データ」とは、ある程度の構造（キーと値のペアや階層構造など）を持っていますが、リレーショナルデータベースのような厳密な「表形式」には従っていないデータのことです。JSON、Parquet、XML、Avroなどがこれに該当します。

❌ **CSV**：列と行で構成された厳密な表形式のデータであり、「構造化データ」に分類されます。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題47: ステージにある JSON データをSnowflakeのテーブルに読み込むには？

ステージにある JSON データを Snowflake のテーブルに読み込む（ロードする）にはどうすればよいですか？

- A) `INSERT INTO` コマンドを使用する。
- B) `COPY INTO` コマンドを使用する。
- C) `LOAD DATA` コマンドを使用する。
- D) `UPLOAD JSON` コマンドを使用する。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) `COPY INTO` コマンドを使用する。**

Snowflakeにおいて、内部・外部ステージからデータをテーブルに一括で読み込むための主要コマンドは `COPY INTO` です。これはCSVなどの構造化データだけでなく、JSONやParquetなどの半構造化データのロードにも対応しています。

❌ **`INSERT INTO`**：1行ずつの挿入やクエリ結果の挿入に使い、ステージからのファイルの読み込みには適していません。

❌ **`LOAD DATA / UPLOAD JSON`**：SnowflakeのSQLコマンドとしては存在しません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- テーブルと FILE FORMAT の準備
CREATE TABLE json_data (raw_json VARIANT);
CREATE FILE FORMAT my_json_format TYPE = 'JSON';

-- ステージにある JSON ファイルをテーブルにロード
COPY INTO json_data
FROM @my_stage/data.json
FILE_FORMAT = (FORMAT_NAME = 'my_json_format');

-- ロード結果の確認
SELECT * FROM json_data LIMIT 5;

-- JSON データの中身をパス記法でアクセス
SELECT raw_json:id::INT, raw_json:name::STRING FROM json_data LIMIT 5;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題48: Snowflakeにおける `PARSE_JSON` 関数の目的は何ですか？

Snowflakeにおける `PARSE_JSON` 関数の目的は何ですか？

- A) JSONデータを専用のPARSE形式で保存する。
- B) JSONデータをXML形式に変換する。
- C) 外部ステージからJSONデータを読み込む。
- D) 文字列のJSONデータを解析し、リレーショナルテーブル（SQL）で扱える形式に変換する。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: D) 文字列のJSONデータを解析し、リレーショナルテーブル（SQL）で扱える形式に変換する。**

`PARSE_JSON` 関数は、単なるテキスト（文字列）として入力されたJSONデータを解析し、Snowflakeが半構造化データとして処理できる `VARIANT` 型のオブジェクトに変換します。これにより、パス記法（ドットやブラケット）を用いて、SQLでJSONデータの中身を簡単に検索・抽出できるようになります。

❌ データを保存したり、XMLに変換したり、データをロード（読み込み）したりする機能はありません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- PARSE_JSON で文字列を VARIANT 型に変換
SELECT PARSE_JSON('{"name": "Alice", "age": 30, "city": "Tokyo"}') AS json_obj;

-- 変換後の VARIANT からフィールドを抽出
SELECT
    PARSE_JSON('{"name": "Alice", "age": 30}'):name::STRING AS name,
    PARSE_JSON('{"name": "Alice", "age": 30}'):age::INT AS age;

-- VARIANT 列に格納されたデータへの適用
SELECT raw_json:id::INT, raw_json:name::STRING
FROM json_data LIMIT 5;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題49: Snowflakeで半構造化データ内のネストされたフィールドをクエリするには？（2つ選択）

Snowflakeで半構造化データ内のネストされた（階層の深い）フィールドをクエリ（検索）するにはどうすればよいですか？（2つ選択）

- A) ドット記法（ `.` ）を使用してアクセスする。
- B) Snowflakeの専用関数を使用する。
- C) ブラケット記法（ `[]` ）を使用してアクセスする。
- D) Cloud Functionsを使用する。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) ドット記法（ `.` ）を使用してアクセスする。、C) ブラケット記法（ `[]` ）を使用してアクセスする。**

`VARIANT` 列に格納されたJSONなどの階層データにアクセスするには、パス記法を使用します。

- **ドット記法**（例：`data:location.city`）：コロン `:` でVARIANT列名と最初のキーを区切り、その後はドット `.` で階層をたどります。例えば `{"location": {"city": "Tokyo"}}` というデータなら `data:location.city` で `"Tokyo"` を取得できます。

- **ブラケット記法**（例：`data['location']['city']`）：角括弧を使って同様に階層をたどります。フィールド名に空白や特殊文字が含まれている場合はこちらを使います。

❌ データを抽出するための外部のクラウド機能や、専用の関数を使う必要はなく、直接アクセス可能です。

📖 **参照先:** まとめ/03_データ管理_ロード.md > セクション7（半構造化データ）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- ネストされたJSONデータの準備
CREATE TABLE nested_data (data VARIANT);
INSERT INTO nested_data SELECT PARSE_JSON('{"user": {"name": "Alice", "address": {"city": "Tokyo", "zip": "100-0001"}}, "scores": [90, 85, 95]}');

-- ドット記法でネストされたフィールドにアクセス
SELECT data:user.name::STRING AS name,
       data:user.address.city::STRING AS city
FROM nested_data;

-- ブラケット記法でアクセス
SELECT data['user']['name']::STRING AS name,
       data['user']['address']['city']::STRING AS city
FROM nested_data;

-- 配列要素へのアクセス（インデックス指定）
SELECT data:scores[0]::INT AS score_1,
       data:scores[1]::INT AS score_2
FROM nested_data;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題50: 「非構造化データ」の例はどれですか？（3つ選択）

「非構造化データ」の例はどれですか？（3つ選択）

- A) JSON
- B) Image（画像）
- C) Video（動画）
- D) Pdf（PDFドキュメント）

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) Image（画像）、C) Video（動画）、D) Pdf（PDFドキュメント）**

「非構造化データ」とは、事前に定義されたデータ構造や形式（スキーマ）を持たないデータのことです。画像、音声、動画、自然言語のテキスト、PDFファイルなどがこれに該当し、そのままではデータベースで検索・集計することが困難です。

❌ **JSON**：キーと値のペアという構造を持っているため「半構造化データ」に分類されます。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題51: CSVデータを読み込む際、ヘッダー行を読み飛ばすFILE FORMATオプションは？

CSVデータを読み込む際、見出し行（ヘッダー行）を読み飛ばすための FILE FORMAT オプションはどれですか？

- A) SKIP_FILE_HEADER = FALSE
- B) IGNORE_HEADER = TRUE
- C) HEADER_SKIP = 1
- D) SKIP_HEADER = 1

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: D) SKIP_HEADER = 1**

`COPY INTO` コマンドでCSVを読み込む際、FILE FORMATの設定内で `SKIP_HEADER = 1` を指定すると、ファイルの最初の1行（通常は列名が書かれたヘッダー行）を無視して、2行目からデータの読み込みを開始します。

❌ その他の選択肢はすべて、Snowflakeでは無効な構文やパラメータ名です。

📖 **参照先:** まとめ/03_データ管理_ロード.md > セクション3（COPYコマンド）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- FILE FORMAT でヘッダー行をスキップする設定
CREATE FILE FORMAT csv_with_header
    TYPE = 'CSV'
    SKIP_HEADER = 1          -- 1行目のヘッダーをスキップ
    FIELD_DELIMITER = ','
    NULL_IF = ('NULL', 'null', '');

-- COPY INTO でそのフォーマットを使用
COPY INTO my_table
FROM @my_stage/data.csv
FILE_FORMAT = (FORMAT_NAME = 'csv_with_header');

-- または COPY INTO 内でインラインで指定
COPY INTO my_table
FROM @my_stage/data.csv
FILE_FORMAT = (TYPE = 'CSV' SKIP_HEADER = 1);
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題52: `DESCRIBE TABLE` コマンドを実行すると得られる情報は？（3つ選択）

Snowflakeでテーブルの構造を記述するコマンド（ `DESCRIBE TABLE` ）を実行すると、どのような情報が得られますか？（3つ選択）

- A) 列（カラム）の名前とデータ型
- B) テーブルの制約（Constraints）
- C) テーブルの作成元（Table Origin）
- D) インデックスとキー（Indexes and keys）

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) 列（カラム）の名前とデータ型、B) テーブルの制約（Constraints）、D) インデックスとキー（Indexes and keys）**

`DESCRIBE TABLE <テーブル名>` コマンドは、対象テーブルのメタデータ（構成情報）を返します。これには、各列の名前、データ型（VARCHAR、NUMBERなど）、列に設定された制約（NOT NULLなど）、主キー（PRIMARY KEY）の設定などの情報が含まれます。

❌ **テーブルの作成元（Table Origin）**：どこからデータが作られたかといった由来の情報は表示されません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- テーブルの構造を確認
DESCRIBE TABLE my_table;
-- または省略形
DESC TABLE my_table;

-- 表示される情報：
-- name（列名）、type（データ型）、kind（列の種類）、null?（NULL許容）、
-- default（デフォルト値）、primary key（主キー設定）等を確認する

-- 実際のテーブルを作成して試す
CREATE TABLE sample_table (
    id INT NOT NULL PRIMARY KEY,
    name VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP()
);
DESCRIBE TABLE sample_table;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題53: Snowflakeにおいて `LIST` コマンドと同じ意味を持つ短縮コマンドは？

Snowflakeにおいて `LIST` コマンドと同じ意味を持つ（短縮形の）コマンドはどれですか？

- A) LS
- B) SHOW
- C) DESC
- D) LIS

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) LS**

`LIST` コマンドは、ステージ内に保存されているファイルの一覧（ファイル名、サイズ、更新日時など）を表示するために使用されます。このコマンドのエイリアス（短縮形）として `LS` が用意されており、Linuxなどのコマンドライン操作に慣れているユーザーにとって直感的に使用できるようになっています。

❌ **SHOW**：テーブルやユーザーなどのオブジェクト一覧を表示します。

❌ **DESC**：テーブルなどの構造を詳細表示します。

❌ **LIS**：無効なコマンドです。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- ステージ内のファイルを LIST コマンドで確認
LIST @my_stage;

-- 短縮形 LS でも同じ結果が得られることを確認
LS @my_stage;

-- ユーザーステージの確認
LIST @~;
LS @~;

-- 特定のパスのみを絞り込む
LIST @my_stage/2026/;

-- ファイルを削除
REMOVE @my_stage/old_file.csv;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題54: ステージにあるファイルへの「事前署名付きURL」を生成する関数は？

Snowflakeのステージにあるファイルにアクセスするための「事前署名付きURL（一時的にアクセス可能なURL）」を生成する関数はどれですか？

- A) GET_STAGE_LOCATION()
- B) BUILD_STAGE_FILE_URL()
- C) GET_PRESIGNED_URL()
- D) GET_RELATIVE_PATH()

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: C) GET_PRESIGNED_URL()**

`GET_PRESIGNED_URL()` 関数を使用すると、内部または外部ステージにある特定のファイルへのアクセスを許可する「有効期限付きのURL」を生成できます。ステージ全体へのアクセス権限を与えなくても、このURLを知っている人やアプリケーションだけが、指定された期間だけファイルをダウンロードできるようになります。

❌ **BUILD_STAGE_FILE_URL()**：一時的なものではなく、永続的なファイルURLを構築します。

❌ 他の2つは場所やパスを取得する関数であり、アクセス用のURLを発行するものではありません。

📖 **参照先:** まとめ/03_データ管理_ロード.md > セクション2（ステージ）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- 事前署名付きURL（有効期限付きの一時アクセスURL）を生成
SELECT GET_PRESIGNED_URL(@my_stage, 'my_file.csv', 3600) AS presigned_url;
-- 3600 = 有効期限（秒）。このURLを知っている人は1時間だけダウンロード可能

-- 永続的なファイルURL（ステージのパーミッション依存）
SELECT BUILD_STAGE_FILE_URL(@my_stage, 'my_file.csv') AS file_url;

-- ステージの場所を取得
SELECT GET_STAGE_LOCATION(@my_stage) AS stage_location;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題55: Snowflakeから半構造化データをアンロードする際にサポートされるデータ形式は？（2つ選択）

Snowflakeから半構造化データを外部に書き出す（アンロードする）際にサポートされているデータ形式はどれですか？（2つ選択）

- A) JSON
- B) CSV
- C) Parquet
- D) XML

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) JSON、C) Parquet**

Snowflakeからデータを抽出してステージに出力（アンロード）する際、半構造化データ形式として JSON および Parquet 形式での出力をサポートしています。

❌ **CSV**：サポートされていますが、これは「構造化データ」フォーマットです。

❌ **XML**：Snowflakeへの読み込みはサポートされていますが、XML形式でのアンロード（書き出し）はサポートされていません。

📖 **参照先:** まとめ/03_データ管理_ロード.md > セクション2（ステージ）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題56: Snowflake Cortexで、顧客レビューの「感情分析」に使用する関数は？

Snowflake Cortexで、顧客レビューの「感情分析（ポジティブかネガティブか）」を判定するために使用する関数はどれですか？

- A) TRANSLATE
- B) COMPLETE
- C) SENTIMENT
- D) PARSE_DOCUMENT

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: C) SENTIMENT**

Snowflake Cortexの `SENTIMENT` 関数は、入力されたテキスト（英語に対応）の内容を分析し、その感情を `-1.0`（ネガティブ）から `1.0`（ポジティブ）の範囲のスコアとして数値化して返します。0に近いほど中立な感情と判定されます。レビューやアンケートの分析に最適です。

❌ **TRANSLATE**：テキストを別の言語に翻訳する関数です。感情の判定はしません。

❌ **COMPLETE**：LLM（大規模言語モデル）にプロンプト（指示文）を渡し、自由形式のテキストを生成する関数です。感情スコアの数値出力はできません。

❌ **PARSE_DOCUMENT**：PDFやWordなどのドキュメントからテキストやレイアウト情報を抽出する関数です。感情分析は行いません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- SENTIMENT 関数で感情スコアを取得（-1.0 ネガティブ ～ 1.0 ポジティブ）
SELECT SNOWFLAKE.CORTEX.SENTIMENT('This product is absolutely amazing!') AS score;
-- → 0.8 程度のポジティブなスコアが返る

SELECT SNOWFLAKE.CORTEX.SENTIMENT('The service was terrible and very disappointing.') AS score;
-- → -0.7 程度のネガティブなスコアが返る

-- テーブル内のレビューデータに一括適用
SELECT review_text,
       SNOWFLAKE.CORTEX.SENTIMENT(review_text) AS sentiment_score
FROM customer_reviews
LIMIT 10;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題57: Snowflake Cortexにおける `COMPLETE` 関数の目的は何ですか？

Snowflake Cortexにおける `COMPLETE` 関数の目的は何ですか？

- A) ドキュメントを解析するため。
- B) 言語を翻訳するため。
- C) テキストを分類するため。
- D) 与えられた指示（プロンプト）に基づいてテキストを生成するため。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: D) 与えられた指示（プロンプト）に基づいてテキストを生成するため。**

`COMPLETE` 関数は、大規模言語モデル（LLM）を利用して、ユーザーが入力したプロンプトに対する回答（補完テキスト）を生成する機能です。要約の作成、質問応答、コードの生成など、汎用的なテキスト生成タスクに使用されます。

❌ ドキュメント解析は `PARSE_DOCUMENT`、翻訳は `TRANSLATE`、テキスト分類は `CLASSIFY_TEXT` の役割です。

📖 **参照先:** まとめ/06_Cortex_AI_ML.md > セクション1（Cortex AI）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- COMPLETE 関数でテキスト生成（要約・質問応答・コード生成など）
SELECT SNOWFLAKE.CORTEX.COMPLETE(
    'llama3-8b',
    '以下のテキストを3行で要約してください：Snowflakeはクラウドデータプラットフォームであり...'
) AS summary;

-- テーブルデータの要約に適用
SELECT product_name,
       SNOWFLAKE.CORTEX.COMPLETE(
           'mistral-7b',
           CONCAT('この商品の特徴を一文で説明してください：', description)
       ) AS one_line_summary
FROM products
LIMIT 5;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題58: `TRANSLATE` 関数で翻訳元・翻訳先の言語を指定する入力形式は？

`TRANSLATE` 関数を使用する際、翻訳元（ソース）と翻訳先（ターゲット）の言語を指定するために想定される入力形式はどれですか？

- A) 言語の完全な名称（例："English", "French"）。
- B) 標準の言語コード（例："en", "fr"）。
- C) 言語辞書へのファイルパス。
- D) 数値による言語識別子。

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) 標準の言語コード（例："en", "fr"）。**

Cortexの `TRANSLATE` 関数で言語を指定する際は、ISO標準の言語コード（2文字など）を使用します。

例：`SELECT SNOWFLAKE.CORTEX.TRANSLATE('Hello', 'en', 'fr');` （英語からフランス語へ）

※翻訳元の言語を自動検出させたい場合は、ソース言語に空の文字列 `''` を指定します。

❌ "English" のような完全な単語や、数字のID、外部ファイルのパスなどは使用しません。

📖 **参照先:** まとめ/06_Cortex_AI_ML.md > セクション1（Cortex AI）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- TRANSLATE 関数で翻訳（ソース言語コード → ターゲット言語コード）
SELECT SNOWFLAKE.CORTEX.TRANSLATE('Hello, how are you?', 'en', 'ja') AS translation;
-- → 「こんにちは、お元気ですか？」等が返る

-- 英語からフランス語へ
SELECT SNOWFLAKE.CORTEX.TRANSLATE('Good morning', 'en', 'fr') AS translation;

-- ソース言語を自動検出（空文字列を指定）
SELECT SNOWFLAKE.CORTEX.TRANSLATE('Bonjour le monde', '', 'en') AS translation;

-- テーブルのデータに一括適用
SELECT original_text,
       SNOWFLAKE.CORTEX.TRANSLATE(original_text, 'ja', 'en') AS english_text
FROM messages LIMIT 5;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題59: `PARSE_DOCUMENT` 関数で処理できるファイル形式はどれですか？（3つ選択）

`PARSE_DOCUMENT` 関数で処理できるファイル形式はどれですか？（3つ選択）

- A) PDF
- B) PPTX
- C) XLSX
- D) DOCX

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) PDF、B) PPTX、D) DOCX**

Snowflake Cortexの `PARSE_DOCUMENT` 関数は、非構造化ドキュメントからテキストやレイアウト情報を抽出します。現在、PDFドキュメントに加えて、Microsoft Office形式である PPTX（PowerPoint）や DOCX（Word）の解析をサポートしています。

❌ **XLSX（Excel）**：現時点では `PARSE_DOCUMENT` 関数のサポート対象外です。

📖 **参照先:** まとめ/06_Cortex_AI_ML.md > セクション1（Cortex AI）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題60: `PARSE_DOCUMENT` 関数で利用可能な「モード」はどれですか？（2つ選択）

`PARSE_DOCUMENT` 関数で利用可能な「モード」はどれですか？（2つ選択）

- A) OCR
- B) OMR
- C) CONTENT
- D) LAYOUT

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) OCR、D) LAYOUT**

`PARSE_DOCUMENT` 関数には、ドキュメントの解析方法として以下の2つのモードが用意されています。

- **OCR モード**：OCR（Optical Character Recognition：光学文字認識）技術を用いて、スキャンされた画像やPDFから純粋なテキスト情報を抽出します。スキャン文書など、デジタルテキストとして埋め込まれていない文字を画像として認識・読み取る技術です。
    
- **LAYOUT モード**：テキストだけでなく、ドキュメント内の見出し、段落、表（テーブル）などのレイアウト構造も一緒に抽出・保持します。
    

❌ **OMR / CONTENT**：これらはサポートされているモード名ではありません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題61: `COPY INTO <テーブル名>` コマンドでファイル形式を指定するパラメータは？

`COPY INTO <テーブル名>` コマンドを使用する際、ファイルのフォーマット（形式）を指定するために使用できるパラメータはどれですか？

- A) FILE_FORMAT
- B) DATA_TYPE
- C) FILE_TYPE
- D) LOAD_FORMAT

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: A) FILE_FORMAT**

`COPY INTO` コマンドでデータを読み込む際、`FILE_FORMAT` パラメータを使用して「どのような形式のデータを読み込むのか」を指定します。あらかじめ定義しておいたフォーマット名（例：`my_csv_format`）を指定するか、またはその場で `TYPE = CSV` などのオプションを直接記述することができます。

❌ その他の選択肢はすべて無効なパラメータ名です。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- FILE_FORMAT を使った COPY INTO コマンド
-- 方法1: あらかじめ定義した FILE FORMAT オブジェクトを参照
CREATE FILE FORMAT my_csv_format
    TYPE = 'CSV'
    FIELD_DELIMITER = ','
    SKIP_HEADER = 1;

COPY INTO my_table
FROM @my_stage/data.csv
FILE_FORMAT = (FORMAT_NAME = 'my_csv_format');

-- 方法2: COPY INTO 内でインラインで FILE_FORMAT を直接指定
COPY INTO my_table
FROM @my_stage/data.csv
FILE_FORMAT = (TYPE = 'CSV' FIELD_DELIMITER = ',' SKIP_HEADER = 1);

-- ロード状況の確認
SELECT * FROM TABLE(INFORMATION_SCHEMA.COPY_HISTORY(TABLE_NAME => 'MY_TABLE', START_TIME => DATEADD(hours, -1, CURRENT_TIMESTAMP())));
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題62: `COPY INTO <テーブル名>` コマンドでCSVの「区切り文字」を指定するオプションは？

`COPY INTO <テーブル名>` コマンドで、CSVファイルの「区切り文字」を指定するために使用できるオプションはどれですか？

- A) DELIMITER
- B) SEPARATOR
- C) FIELD_DELIMITER
- D) COLUMN_DELIMITER

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: C) FIELD_DELIMITER**

CSVデータをロードする際、データとデータを区切っている文字（カンマ `,` や タブ `\t` など）を指定するためのオプションは `FIELD_DELIMITER` です。これを正しく設定することで、Snowflakeはデータを正確な列に分割して読み込むことができます。

❌ その他の選択肢はSnowflakeでは無効なオプション名です。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- FIELD_DELIMITER で区切り文字を指定
CREATE FILE FORMAT tab_delimited
    TYPE = 'CSV'
    FIELD_DELIMITER = '\t'  -- タブ区切り
    SKIP_HEADER = 1;

CREATE FILE FORMAT pipe_delimited
    TYPE = 'CSV'
    FIELD_DELIMITER = '|'   -- パイプ区切り
    SKIP_HEADER = 1;

-- カンマ区切り（デフォルト）のCSV
CREATE FILE FORMAT comma_csv
    TYPE = 'CSV'
    FIELD_DELIMITER = ','
    FIELD_OPTIONALLY_ENCLOSED_BY = '"'  -- 引用符で囲まれたフィールドにも対応
    SKIP_HEADER = 1;

-- ファイルフォーマットの一覧確認
SHOW FILE FORMATS;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題63: 仮想ウェアハウスに対して設定できる項目はどれですか？（3つ選択）

Snowflakeの仮想ウェアハウスに対して設定できる項目はどれですか？（3つ選択）

- A) クラウドプロバイダーのリージョン（地域）
- B) 自動停止までの時間 (Auto-suspend time)
- C) 自動再開 (Auto-resume)
- D) ウェアハウスのサイズ (Warehouse size)

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) 自動停止までの時間 (Auto-suspend time)、C) 自動再開 (Auto-resume)、D) ウェアハウスのサイズ (Warehouse size)**

仮想ウェアハウスのパフォーマンスとコスト効率を最適化するために、サイズ（処理能力）、自動停止（一定時間使用されない場合のアイドル停止）、および自動再開（クエリ実行時の自動起動）を自由に設定・変更できます。

❌ **クラウドプロバイダーのリージョン**：アカウント全体の設定であり、ウェアハウスごとに変更することはできません。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション3（Virtual Warehouse）

</details>

### 実際に確認してみよう

**Snowsight（UI）での手順:**
1. 左メニュー「Admin」→「Warehouses」でウェアハウスを選択して「Edit」を開く
2. 設定できる項目を確認する：
   - Size（XSmall / Small / Medium / Large / XLarge 等）
   - Auto Suspend（秒単位）
   - Auto Resume（ON/OFF）
   - Min / Max Cluster Count（Enterprise Edition以上）

**SQLでの確認:**
```sql
-- ウェアハウスの複数設定を一度に変更
ALTER WAREHOUSE my_wh SET
    WAREHOUSE_SIZE = 'SMALL'
    AUTO_SUSPEND = 300
    AUTO_RESUME = TRUE;

-- 現在の設定を確認
SHOW WAREHOUSES LIKE 'my_wh';
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題64: ステージからSnowflakeのテーブルに構造化データを読み込むコマンドは？

ステージからSnowflakeのテーブルに構造化データを読み込むために、一般的に使用されるSQLコマンドはどれですか？

- A) INSERT INTO
- B) COPY INTO
- C) LOAD DATA
- D) IMPORT DATA

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: B) COPY INTO**

（※問題47と同内容の質問です）

Snowflakeにおいて、大量のデータをステージからテーブルに効率的かつ高速に一括読み込み（バルクロード）するための標準的なコマンドは `COPY INTO` です。

❌ **INSERT INTO**：データの大量ロードには適していません。

❌ **LOAD DATA / IMPORT DATA**：無効なコマンドです。

📖 **参照先:** まとめ/01_アーキテクチャ基礎.md > セクション7（オブジェクト階層）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- 構造化データ（CSV）のバルクロード
COPY INTO sales_data
FROM @my_stage/sales_2026.csv
FILE_FORMAT = (TYPE = 'CSV' FIELD_DELIMITER = ',' SKIP_HEADER = 1)
ON_ERROR = 'CONTINUE';  -- エラーがあっても続行

-- ロード履歴の確認（直近1時間）
SELECT *
FROM TABLE(INFORMATION_SCHEMA.COPY_HISTORY(
    TABLE_NAME => 'SALES_DATA',
    START_TIME => DATEADD(hours, -1, CURRENT_TIMESTAMP())
));

-- ステージ内の全CSVを一括ロード
COPY INTO sales_data
FROM @my_stage/
PATTERN = '.*\.csv'
FILE_FORMAT = (TYPE = 'CSV' SKIP_HEADER = 1);
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題65: ステージにあるファイルへの「事前署名付きURL」を生成する関数は？（問題54と同内容）

Snowflakeのステージにあるファイルにアクセスするための「事前署名付きURL」を生成する関数はどれですか？

- A) GET_STAGE_LOCATION()
- B) BUILD_STAGE_FILE_URL()
- C) GET_PRESIGNED_URL()
- D) GET_RELATIVE_PATH()

### あなたの回答

```
選択: []

選んだ理由・メモ:


```

### 正解・解説

<details>
<summary>クリックして確認</summary>

**正解: C) GET_PRESIGNED_URL()**

（※問題54と同内容の質問です）

特定のファイルへの安全で一時的なアクセス（ダウンロード）を許可するURLを発行するには、`GET_PRESIGNED_URL()` 関数を使用します。

📖 **参照先:** まとめ/03_データ管理_ロード.md > セクション2（ステージ）

</details>

### 実際に確認してみよう

**SQLでの確認:**
```sql
-- 事前署名付きURLを生成して外部アプリケーションや画像表示に利用
SELECT GET_PRESIGNED_URL(@images_stage, 'photo_001.jpg', 3600) AS download_url;
-- 生成されたURLをブラウザで開くと、1時間以内だけダウンロード可能

-- 複数ファイルのURLを一括生成
SELECT
    relative_path,
    GET_PRESIGNED_URL(@my_stage, relative_path, 1800) AS url
FROM DIRECTORY(@my_stage);

-- 対比：BUILD_STAGE_FILE_URL は永続的なURL（権限が必要）
SELECT BUILD_STAGE_FILE_URL(@my_stage, 'report.pdf') AS permanent_url;
```

### 振り返り

- [ ] 正解できた
- [ ] 間違えた → 復習ポイント:

---

## 問題一覧と解答記録

| # | 問題概要 | ドメイン | 形式 | チェック |
|---|---------|---------|------|----------|
| 1 | Snowflakeのアーキテクチャを構成する3つの主要なレイ | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 2 | Snowflakeはどのようにクエリを処理しますか？ | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 3 | Snowflakeがサポートしているクラウドプラットフォーム | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 4 | Snowflakeはデータを保存するためにどのようなストレー | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 5 | Snowflakeのユーザーインターフェース（UI）は何と呼 | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 6 | Snowflakeノートブックでデータへのクエリや文書化に使 | Snowflakeの操作とアーキテクチャ | 複数 | [ ] |
| 7 | Snowflakeノートブックの主な特徴は何ですか？ | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 8 | Pythonワークシートの設定項目で利用可能なオプションはど | Snowflakeの操作とアーキテクチャ | 複数 | [ ] |
| 9 | Snowflakeノートブックにおいて、セルの横にある赤色の | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 10 | ユーザーはSnowflakeノートブックに Streamli | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 11 | Snowflakeノートブックにおいて、Pythonの変数を | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 12 | Snowsightを使用して、ローカル（自分のパソコン）のフ | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 13 | Snowsightの Query History（クエリ履歴 | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 14 | Snowsightで新しいステージ（ファイルの置き場）を作成 | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 15 | Snowflakeで利用可能な「ビュー」の種類はどれですか？ | Snowflakeの操作とアーキテクチャ | 複数 | [ ] |
| 16 | Snowflakeの「データベース」の中に含めることができな | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 17 | Snowflakeの階層構造において、データベースとスキーマ | Snowflakeの操作とアーキテクチャ | 複数 | [ ] |
| 18 | Snowflakeがサポートしている「数値データ型」はどれで | Snowflakeの操作とアーキテクチャ | 複数 | [ ] |
| 19 | Snowflakeのテーブルの種類として正しいものはどれです | Snowflakeの操作とアーキテクチャ | 複数 | [ ] |
| 20 | Snowflakeを最も正確に表している説明はどれですか？ | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 21 | Snowflakeで新しいデータベースを作成するために使用さ | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 22 | Snowflakeのワークシートで `INSERT`（データ | Snowflakeの操作とアーキテクチャ | 単一 | [ ] |
| 23 | SnowflakeのRBAC（ロールベースアクセス制御）シス | IDとデータアクセス管理 | 複数 | [ ] |
| 24 | Snowflakeにおける「ロールの階層化」の目的は何ですか | IDとデータアクセス管理 | 単一 | [ ] |
| 25 | Snowflakeにおける「カスタムロール」の主な特徴は何で | IDとデータアクセス管理 | 複数 | [ ] |
| 26 | Snowflakeにおいて、ロールはどのようにしてオブジェク | IDとデータアクセス管理 | 複数 | [ ] |
| 27 | Snowflakeで、ロールに権限を付与（許可）するために使 | IDとデータアクセス管理 | 単一 | [ ] |
| 28 | Snowflakeのクエリにおける `LIMIT` 句の主な | IDとデータアクセス管理 | 単一 | [ ] |
| 29 | `SELECT` クエリの検索対象から特定の列（カラム）だけ | IDとデータアクセス管理 | 単一 | [ ] |
| 30 | Snowflakeにおける「完全修飾名（Fully Qual | IDとデータアクセス管理 | 単一 | [ ] |
| 31 | Snowflakeで `DROP SCHEMA IF EXI | IDとデータアクセス管理 | 単一 | [ ] |
| 32 | Snowflakeにおける `PUBLIC` スキーマの目的 | IDとデータアクセス管理 | 複数 | [ ] |
| 33 | Snowflakeの「Cloning（クローン）」機能の主な | データ保護とデータ共有 | 単一 | [ ] |
| 34 | Snowflakeのクローン機能を使用してコピー（複製）でき | データ保護とデータ共有 | 複数 | [ ] |
| 35 | Standard Edition（標準アカウント）のSnow | データ保護とデータ共有 | 単一 | [ ] |
| 36 | Snowflake Marketplace や Data E | データ保護とデータ共有 | 単一 | [ ] |
| 37 | Snowflake Marketplace における「データ | データ保護とデータ共有 | 単一 | [ ] |
| 38 | Snowflake Data Exchange における「プ | データ保護とデータ共有 | 単一 | [ ] |
| 39 | プライベートなデータ共有環境を構築するために必要な「Snow | データ保護とデータ共有 | 単一 | [ ] |
| 40 | Snowflakeにおけるウェアハウスの「スケールイン /  | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 41 | Snowflakeで「マルチクラスターウェアハウス」を使用す | データのロードと仮想ウェアハウス | 複数 | [ ] |
| 42 | Snowflakeで新しい「仮想ウェアハウス」を作成するため | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 43 | 仮想ウェアハウスにおける「自動停止（Auto-suspend | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 44 | マルチクラスターウェアハウスにおける「オートスケール（Aut | データのロードと仮想ウェアハウス | 複数 | [ ] |
| 45 | Snowflakeで利用できる「ステージ（データの置き場）」 | データのロードと仮想ウェアハウス | 複数 | [ ] |
| 46 | 「半構造化データ」のフォーマット（形式）の例はどれですか？（ | データのロードと仮想ウェアハウス | 複数 | [ ] |
| 47 | ステージにある JSON データを Snowflake のテ | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 48 | Snowflakeにおける `PARSE_JSON` 関数の | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 49 | Snowflakeで半構造化データ内のネストされた（階層の深 | データのロードと仮想ウェアハウス | 複数 | [ ] |
| 50 | 「非構造化データ」の例はどれですか？（3つ選択） | データのロードと仮想ウェアハウス | 複数 | [ ] |
| 51 | CSVデータを読み込む際、見出し行（ヘッダー行）を読み飛ばす | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 52 | Snowflakeでテーブルの構造を記述するコマンド（ `D | データのロードと仮想ウェアハウス | 複数 | [ ] |
| 53 | Snowflakeにおいて `LIST` コマンドと同じ意味 | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 54 | Snowflakeのステージにあるファイルにアクセスするため | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 55 | Snowflakeから半構造化データを外部に書き出す（アンロ | データのロードと仮想ウェアハウス | 複数 | [ ] |
| 56 | Snowflake Cortexで、顧客レビューの「感情分析 | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 57 | Snowflake Cortexにおける `COMPLETE | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 58 | `TRANSLATE` 関数を使用する際、翻訳元（ソース）と | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 59 | `PARSE_DOCUMENT` 関数で処理できるファイル形 | データのロードと仮想ウェアハウス | 複数 | [ ] |
| 60 | `PARSE_DOCUMENT` 関数で利用可能な「モード」 | データのロードと仮想ウェアハウス | 複数 | [ ] |
| 61 | `COPY INTO <テーブル名>` コマンドを使用する際 | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 62 | `COPY INTO <テーブル名>` コマンドで、CSVフ | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 63 | Snowflakeの仮想ウェアハウスに対して設定できる項目は | データのロードと仮想ウェアハウス | 複数 | [ ] |
| 64 | ステージからSnowflakeのテーブルに構造化データを読み | データのロードと仮想ウェアハウス | 単一 | [ ] |
| 65 | Snowflakeのステージにあるファイルにアクセスするため | データのロードと仮想ウェアハウス | 単一 | [ ] |

---

> 学習のヒント: 間違えた問題は「振り返り」欄にメモを記入し、まとめファイルで復習しましょう。
