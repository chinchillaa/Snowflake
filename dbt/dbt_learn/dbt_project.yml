name: 'dbt_learn'
version: '1.0.0'

profile: 'dbt_learn'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_learn:
    staging:
      +materialized: view
    marts:
      +materialized: table
